[workspace]
members = ["vm-core", "vm-bin", "vm-db", "hide-macro"]
resolver = "3"

[workspace.profile.release]
lto = "fat"
opt-level = 3
codegen-units = 1
strip = true
panic = "abort"
incremental = true