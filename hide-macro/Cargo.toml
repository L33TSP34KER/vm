# hide-macro/Cargo.toml
[package]
name = "hide-macro"
version = "0.1.0"
edition = "2021"


[dependencies]
syn = { version = "2", features = ["full"] }
quote = "1"
proc-macro2 = "1"
paste = "1.0"  # optional, for label generation
