# hide-macro/Cargo.toml
[package]
name = "hide-macro"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true  # ← this is the missing line

[dependencies]
syn = { version = "2", features = ["full"] }
quote = "1"
proc-macro2 = "1"
